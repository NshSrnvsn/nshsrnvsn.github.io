---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Socials from "@/components/Socials.astro";
import { SOCIALS } from "@/constants";
---

<Layout>
  <Header />
  <main id="main-content" data-layout="index" class="app-layout">
    <section id="hero" class:list={["pt-12 pb-6"]}>
      <!---<h1 class="my-4 inline-block text-4xl font-bold sm:my-8 sm:text-5xl">
        Anusha Srinivasan
      </h1>--->
      <p>
        Hi, I’m a senior software engineer who started out building large-scale systems and somehow wandered into machine learning—and never left. I didn’t plan it. I got curious, followed the data, and here we are.
      </p>
      <br />
      <p>
      These days, I’m finishing up my master’s at Georgia Tech, which has been equal parts challenging, humbling, and genuinely fun. ♥
      </p>
      <br />
      <p>
      I love building things.
      </p>
      <br />
      <p>
        Sometimes that’s scalable software, sometimes it’s LEGO creations, and sometimes it’s food experiments that turn out better than expected. I’m currently trying to build a skateboard.
      </p>
      <br />
      <p>
      If you want the serious stuff, check out <a href="public/SrinivasanAnusha_Resume.pdf" class="underline">my work.</a></br>
      If you want the less serious stuff, check out <a href="/archives" class="underline">the things I do</a> and <a href="/blog" class="underline">write about</a>.   
      </p>
      </br>
      <p>
        Hit me up for a chat, I make a mean masala chai and if you’re lucky
        and nearby, you might even get a cup.
      </p>

      {
        // only display if at least one social link is enabled
        SOCIALS.length > 0 && (
          <div class="mt-20 mb-4 flex flex-col items-center justify-center text-center">
            <Socials />
          </div>
        )
      }
    </section>

    <!---{
      featuredPosts.length > 0 && (
        <section
          id="featured"
          class:list={[
            "pt-12 pb-6",
            { "border-b border-border": recentPosts.length > 0 },
          ]}
        >
          <h2 class="text-2xl font-semibold tracking-wide">Featured</h2>
          <ul>
            {featuredPosts.map(data => (
              <Card variant="h3" {...data} />
            ))}
          </ul>
        </section>
      )
    }--->

    <!---{
      recentPosts.length > 0 && (
        <section id="recent-posts" class="pt-12 pb-6">
          <h2 class="text-2xl font-semibold tracking-wide">Recent Posts</h2>
          <ul>
            {recentPosts.map(
              (data, index) =>
                index < SITE.postPerIndex && <Card variant="h3" {...data} />
            )}
          </ul>
        </section>
      )
    }--->
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset?.layout;
    if (indexLayout) {
      sessionStorage.setItem("backUrl", "/");
    }
  });
</script>
